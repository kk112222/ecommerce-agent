# 13 · 项目缺陷清单与优化建议

> 评审日期：2026-09-13　评审范围：backend / frontend / scripts / tests / docs
> 评审方式：**全量代码通读 + 跑 pytest + 三个实证探针**（工具错误处理、记忆相似度阈值、文档落盘行为）
> 状态：**19 条全部已修（2026-09-13 收尾完成）**，修复细节见 `CLAUDE.md` 的「增补（09-13）」与各条 `- **状态**` 行。
> 补记（09-14）：修复轮**之后**又自查出 2 条（planner 死循环、子任务异常未隔离），同样已修完并补了回归用例 —— 见[第十节](#十修复轮之后新发现的两条2026-09-14)。原 19 条的编号与结论保持不动，这样"哪条是评审提的、哪条是后来自己发现的"分得清。
> 遗留一处**外部阻塞**：P3-18 的完整数值运行因 DashScope 账户欠费（API 返回 `Arrearage`）跑不完整 ——
> 脚本、离线判据、降级路径都已就位，账户恢复后重跑即可拿到数字。
> 用法：每条缺陷都带【现象 / 证据 / 影响 / 怎么修 / 状态】，可直接当修复待办；文末有"面试谈资"一节。

---

## 零、结论摘要

| 严重度 | 含义 | 条数 | 已修 |
|---|---|---|---|
| 🔴 P0 | 会炸 / 安全 / 功能半截 | 3 | 3 |
| 🟠 P1 | 数据正确性、功能断链、配置失效 | 4 | 4 |
| 🟡 P2 | 可靠性 / 成本 / 性能 | 9 | 9 |
| 🔵 P3 | 测试、评测、文档 | 3 | 3（P3-18 数字待重跑） |
| **合计** | | **19** | **19** |

**一句话结论**：架构与抽象持续变好（上轮点出的 registry 洞、prompt 硬编码、RAG 评测缺失都已闭环），但**新功能带出了新缺陷**，且风险最高的地方（记忆合并、工具异常路径）恰好是测试没有覆盖的地方。
**修复后一句话**：19 条已全部落地并逐条验证（测试从 24 项扩到 **105 项 / 14 个文件**，全离线可跑）；修复过程本身也印证了评审的直觉 —— P0-2、P1-4 恰好是补测试时才敢动的地方。修复轮之后又自查出 2 条并修掉（第十节），测试数即由此 +2。

---

## 一、本次评审的验证记录（结论可信度）

| 验证项 | 命令 / 方式 | 结果 |
|---|---|---|
| 测试套件 | `PYTHONPATH=. .venv/Scripts/python -m pytest -q` | **24 passed**；另 9 个依赖 `tmp_path` 的用例在评审环境报 WinError 32/5（沙箱临时目录限制，**非项目缺陷**），其覆盖行为已手工验证全过（见下） |
| 手工验证 doc_output | 直接调 safe_output_path / write_document | 目录穿越、非法字符清洗、盘符剥离、格式白名单、用户/会话隔离、md/txt/docx 三种落盘 **9/9 通过** |
| 工具错误处理探针 | `registry.execute` 四种输入 | 工具名不存在 ✅提示、缺参数 ✅提示并附参数说明、多传键 ✅过滤、**参数格式非法 ❌抛 ValueError 穿透**（修复轮已转 ✅，见 P0-2 / P2-14） |
| 记忆相似度探针 | `store._ratio` 六组对比 | 见 P1-4 实测表 |

> 修复轮复验（2026-09-13）：`pytest -q` → **104 passed / 14 个文件，全离线**；上表里因沙箱临时目录报 WinError 的用例这次也全过，确认当时判断"非项目缺陷"是对的。
> 再复验（2026-09-14）：补完第十节那 2 条的回归用例后 → **105 passed / 14 个文件**。

---

## 二、🔴 P0 —— 会炸 / 安全

### P0-1 全局关闭 TLS 证书校验（上轮已提，仍在）

- **现象**：`backend/core/llm/qwen.py` 第 18-28 行 monkey-patch 了整个 `requests.Session.request`，给进程内**所有 HTTPS 请求**加上 `verify=False`。
- **证据**：`qwen.py:24` 定义 `_patched_request`，`qwen.py:28` 执行 `requests.Session.request = _patched_request`；注释自称"临时方案…上线前务必删除"。
- **影响**：DashScope API Key 走不校验证书的连接 → 中间人可截获；且**影响范围是全进程**，不止 LLM 调用。任何有经验的评审者看到这一行，会直接判定"这代码不能上生产"。
- **怎么修**：删除 monkey-patch；若确有本地证书问题，改为按请求传 CA bundle / `verify=<path>`，或单独建一个带 verify 配置的 Session。
- **状态**：✅ 已修（2026-09-13）

### P0-2 工具内部异常穿透 registry，打死整个 ReAct 循环（已实测复现）

- **现象**：
  ```
  await reg.execute("query_sales", start_date="本周", end_date="今天")
  → RAISED ValueError: time data '本周' does not match format '%Y-%m-%d'
  ```
- **证据**：`registry.py:57` 是 `return await tool.execute(**clean)` —— 只兜住了"工具名不存在 / 缺必需参数"，**没有 try/except 包住工具本体**；`simple_agent.py:44` 的 ReAct 循环里对 `registry.execute` 同样没有 try。
- **影响**：异常一路上抛 → executor 子任务死 → 整轮对话返回"分析失败"。**这恰好命中《10 · 踩坑总结》里的坑 1（LLM 无时钟感、乱传日期）**：LLM 最常犯的错，现在不是"看到错误自我纠正"，而是整条链路崩。日期格式只是例子，任何工具内部的 ValueError / TypeError / 网络错误都一样。
- **怎么修**：在 `registry.execute` 里包一层
  ```python
  try:
      return await tool.execute(**clean)
  except Exception as e:
      return ToolResult(success=False, data=None,
                        error=f"调用 {name} 失败：{type(e).__name__}: {e}。请检查参数后重试。")
  ```
  配合已有的 `_result_text`（把 error 回传 LLM），LLM 就能自己改参数重试 —— 这才是 Agent 该有的韧性。
- **状态**：✅ 已修（2026-09-13，当时定的第一优先）

### P0-3 文档功能是"半截的"：生成的文档用户拿不到

- **现象**：Agent 回复"已保存到 outputs/xxx.md"，但网页上既看不到也点不开。
- **证据**：`doc_output.py` 写到项目根 `outputs/{user_x}/{session}/`；grep 整个 `backend/` 无 `FileResponse` / `StaticFiles` / 下载类路由；grep `frontend/src` 无任何下载/文档入口；`.gitignore` 里**没有 `outputs/`**（`Test-Path outputs` = True，目录已存在）。
- **影响**：① 功能不可用（用户只能去服务器磁盘找文件）；② 生成物可能被误提交进 git；③ 写进应用目录 → 容器化部署重启即丢。
- **怎么修**：加 `GET /api/documents/{filename}` 下载接口（**必须校验文件归属当前 user_id，防越权下载**）+ 前端在报告里渲染下载链接；`.gitignore` 加 `outputs/`；长期把存储换成对象存储（`infrastructure/oss/` 目录已经在等着）。
- **状态**：✅ 已修（2026-09-13）

---

## 三、🟠 P1 —— 数据正确性 / 功能断链 / 配置失效

### P1-4 记忆合并：作废无护栏 + 相似度判据判别力不足

- **现象 A（无护栏）**：合并规则是"旧行没被任何新句认领 = 判定不再成立 → 作废"（`store.py:186-188`）。这**完全依赖 LLM 每次输出的全量画像都完整**：LLM 少写一条、输出被截断 → 那条记忆被静默作废，没有任何"本轮作废比例异常就拒绝/告警"的保护。
- **现象 B（判据弱）**：`_ratio` 用 difflib 字面相似度分三档（≥0.9 同条 / 0.45~0.9 变化版 / <0.45 新增）。实测：

  | 对比 | ratio | 落档 | 是否符合预期 |
  |---|---|---|---|
  | 负责男装类目 vs 负责女装类目 | 0.833 | 变化版（作废旧行+写新） | ✅ 符合 |
  | 关注退货率 vs 关注退款率 | 0.800 | 变化版 | ⚠️ 两条**平行** KPI 会被反复"作废重写"，id/importance/向量每轮重来；某轮 LLM 只输出一条 → 另一条真被删 |
  | 不关注美妆 vs 关注美妆 | 0.889 | 变化版 | ⚠️ 语义相反也落同一档，说明它分不出"相关"与"相反" |
  | 关注转化率 vs 关注客单价 | 0.400 | 新增 | ✅ 符合 |

- **影响**：记忆抖动（churn）+ 静默丢记忆；`importance` 因每轮重写而永远攒不起来（与 P2-8 叠加）。
- **怎么修**：① 加护栏——单轮"作废数 / 原生效数"超阈值（如 50%）时拒绝本轮合并并记日志；② 判据升级——0.45~0.9 区间交给向量相似度或让 LLM 判"同一偏好的更新 vs 两条不同偏好"；③ 给合并逻辑补 DB 级测试（见 P3-17）。
- **状态**：✅ 已修（2026-09-13）

### P1-5 SQLite 与 Qdrant 之间没有原子性、也没有对账机制

- **现象**：写路径顺序是**先动向量（删作废 / 插新）再提交 SQLite**。
- **证据**：`store.py:202-210`（`delete_points` / `upsert_docs`）早于 `store.py:215-230`（DB 事务 commit）。
- **影响**：
  - 向量已删、DB 提交失败 → 该行永久失去向量，**召回静默降级**（不报错，用户永远看不到它）；
  - 向量已插、DB 提交失败 → **孤儿向量**永久残留（`pid2row` 查不到会跳过，属泄漏）；
  - 设计文档声称"SQLite 是唯一事实源，qdrant 只当索引"，但代码没有让这句话成立（无启动对账、无孤儿清理）。
- **怎么修**：调换顺序（先 commit DB 再同步向量，失败可重试）**或**加 `scripts/reconcile_memory.py`（比对 `rows.qdrant_point_id` 与 qdrant 实际点，双向补/删），并把对账挂到启动或定时任务。
- **状态**：✅ 已修（2026-09-13）

### P1-6 Executor 的两个老洞都还在

- **现象 A**：planner 输出的 `tool_hint` 依然是**死字段** —— `executor.py:16` 只用了 `task['task']`。4 个并行子任务仍能看到**全部工具**，只靠 prompt 软约束，职责泄漏未堵。
- **现象 B**：`executor.run(self, task)`（`executor.py:10`）**不接收 `uploaded_data`**，所以"结合上传的竞品数据分析"这类子任务手里没有竞品数据 —— 文档链路已经能拿到 `uploaded_data` 了，分析链路仍断链。
- **怎么修**：`executor.run(task, uploaded_data=..., user_profile=...)`；按 `tool_hint` 过滤工具集（`get_all_specs(only=[tool_hint])`，需给 registry 加个参数），hint 为空时再放开全集。
- **状态**：✅ 已修（2026-09-13）

### P1-7 数据库配置：echo 常开 + 两个配置字段是死的

- **现象 A**：`session.py:10` 是 `create_async_engine(DB_URL, echo=True)` —— 跑探针时日志刷满 SQL（含业务数据）。
- **现象 B**：`session.py:6-7` 硬编码数据库路径，`config.py:9` 的 `database_url` **从未被使用**（grep 确认只在 config.py 出现）；`config.py:11` 的 `jwt_expire_minutes` 同样失效（`security.py:8` 自己写死 `60*24`）。
- **影响**：① 生产日志既吵又慢还泄数据；② 在 `.env` 改这两项**完全无效** —— 与"配置驱动"的设计目标自相矛盾，且会让"切 PostgreSQL"变成静默失败。
- **怎么修**：`create_async_engine(settings.database_url, echo=settings.debug)`；`security.py` 改读 `settings.jwt_expire_minutes`。
- **状态**：✅ 已修（2026-09-13）

---

## 四、🟡 P2 —— 可靠性 / 成本 / 性能

| # （✅=已修） | 缺陷 | 证据 | 影响 | 怎么修 |
|---|---|---|---|---|
| P2-8 ✅ | **importance 只增不减**：每次召回命中就 +1、封顶 10、无衰减；合并 keep 路径也 +1 | `store.py:322-328`、`store.py:216-220` | 聊十来轮后所有记忆饱和成 10，"重要性"退化成噪声 | 加时间衰减，或每 N 次召回才 +1 |
| P2-9 ✅ | **召回兜底污染上下文**：向量命中不足时按"最近"补齐到 top_k，且这些行同样被 +1 强化 | `store.py:310-316` | 与问题无关的（尤其 episodic）记忆被塞进每轮 prompt，并自我强化 | 兜底只补 semantic，或加相似度下限，且兜底行不计热度 |
| P2-10 ✅ | **episodic 永不过期**：`expires_at` 字段定义了，但全项目无写入/读取 | `user_memory.py:34`（grep 仅此一处） | 情景记忆无限增长，长期污染召回 | 写入 episodic 时给 TTL（如 30 天），读取端已有过期过滤逻辑 |
| P2-11 ✅ | **没有 token / 轮次预算**：intent + planner + 4×executor(≤10 轮) + synthesizer，最坏 40+ 次 LLM 调用；`TokenUsage` 定义了却从未汇总 | `simple_agent.py:21`、`llm/base.py` | 成本与延迟不可控，面试官必问"上线怎么控成本" | Graph 加全局 token/时间预算 + 把 usage 汇总进 report 事件展示 |
| P2-12 ✅ | **知识库改了进程内不生效**：retriever 是模块级单例，BM25 语料只加载一次 | `rag_search.py:8-28` | 重建 KB 后召回旧语料，必须重启后端（演示时容易翻车） | 按语料 mtime/版本号做缓存失效，或提供 reload 接口 |
| P2-13 ✅ | **planner 输出没做结构校验**：只判 `isinstance(list)`；兜底 id 写成 `"tl"`；id 重复/缺字段会让 `zip` 塌缩、results 丢项；空 plan 直接进 synthesizer | `planner.py:47-49`、`supervisor.py:60` | 规划异常时报告缺块或崩，且难排查 | 校验并补全 id/task，兜底改 `t1`，空 plan 降级为单任务 |
| P2-14 ✅ | **工具参数只校验"有没有"，不校验"类型/格式"** | `registry.py:46-57` | 日期传"本周"、word_count 传字符串都能进到工具内部 → 触发 P0-2 崩溃 | 按 spec 的 type 做轻量校验，或各工具自行校验并返回可读错误 |
| P2-15 ✅ | **CORS 全开 + 弃用 API + 非迁移建表**：`allow_origins=["*"]` 对着带 JWT 的接口；`@app.on_event("startup")` 已弃用（应用 lifespan）；pytest 还报了 Pydantic v2 弃用警告 | `app.py:29,40`、`config.py:13` | 安全边界过宽；升级 FastAPI/Pydantic 时会集中爆警告 | CORS 白名单、迁到 lifespan、`Config` 改 `ConfigDict` |
| P2-16 ✅ | **提炼任务不可靠**：`_EXTRACT_LOCKS` 只增不减（长跑内存泄漏）；`asyncio.create_task` 返回值未持有（任务可能被 GC），进程重启/崩溃则本轮记忆提炼静默丢失 | `chat.py:23,58,92` | 记忆时有时无，且不可观测 | 持引用集合 + 失败落待重试表或上队列，并暴露"提炼失败"计数 |

---

## 五、🔵 P3 —— 测试 / 评测 / 文档

### P3-17 测试覆盖避开了风险最高的地方

- **现状**：`tests/` 5 个文件覆盖 `_ratio/_utcnow`（纯函数）、supervisor 路由接线、registry 参数校验、doc_output 路径安全 —— 这些做得不错。
- **缺口**：`merge_semantic_memories`、`recall_user_memories`、`executor`、`planner`、`synthesizer`、SSE 主链路 **零覆盖** —— 而 P0-2、P1-4、P1-5 全在这片区域。
- **怎么修**：优先补"记忆合并"的 DB 级测试（SQLite 内存库 + 已有 FakeLLM），断言：同义刷新不新增行 / 变化版作废旧行 / 旧行被漏写时不静默清空 / 召回命中与兜底行为。
- **状态**：✅ 已修（2026-09-13）。补齐了缺口区：记忆合并/召回用 SQLite 内存库 + FakeLLM 做 DB 级断言（P1-4/P1-5 的护栏就靠它兜住），synthesizer 与 SSE 主链路各自补了正常路径 + 异常兜底用例（含"图抛异常时流仍要关闭""usage 事件必须在 None 哨兵之前"）。
  测试规模：**24 项 → 104 项、14 个文件，全离线**（`pytest -q` 实测，exit 0；09-14 再补 2 条回归用例后为 105 项，见第十节）。

### P3-18 RAG 评测只到"检索层"

- **现状**：`scripts/rag_eval.py` 51 条 golden set + 四策略对比（BM25 / 向量 / 混合 RRF / 混合+精排）+ Recall@5 / Hit@1 / MRR + 按来源拆分 + 真值完整性自检 —— **这批做得相当扎实**（真值选"预训练背不到"的虚构条款尤其专业）。
- **缺口**：衡量的是"片段有没有被召回"，**不是"回答忠实不忠实 / 有没有幻觉"**；回答层评测仍为零。另外运行前必须停后端（qdrant 本地模式单进程锁）——这本身也是"本地模式不适合上线"的证据。
- **怎么修**：加回答层评测：LLM-as-judge 判"答案是否忠于召回片段、有无片段中不存在的数字"，再抽样人工复核；做一次扰动测试（改 KB 里的运费金额 → 答对即证明真走了检索）。
- **状态**：✅ 已修（2026-09-13），但**数字待重跑**。
  - 新增 `scripts/rag_answer_eval.py`：走**真实生产链路**（ServiceAgent → RAGTool → 混合检索 → 精排），用录制代理截获"LLM 当时真正看到的那几块"，三个指标 = LLM 判事实命中率 + LLM 判忠实度 1-5 + **确定性**的数字越界筛查；扰动测试用**基线对照**（改的是临时集合 `kb_perturb_test`，真实 KB 不动，测完即清理）。
  - 离线判据已钉在 `tests/test_rag_answer_eval.py`（7 项全过），LLM 不可用时脚本会逐条跳过并说明原因，不再崩在第一条。
  - **一处更正**：本条写的"回答层评测仍为零"当时已过时 —— `scripts/rag_quality_eval.py` 早就是回答层评测（V1 换值 / V2 清空扰动 + judge 忠实度，6 条精选条款，docs/12 记了 6/6），只是绕过检索栈、手工拼 context。两者对象不同（隔离式 vs 端到端），故都保留，区别写在 `docs/12` 4.3/4.4。
  - **未完成的部分**：完整数值运行卡在 DashScope 账户欠费（`RuntimeError: DashScope API 400 … "type":"Arrearage"`），这是外部阻塞不是代码问题；账户恢复后 `PYTHONPATH=. .venv/Scripts/python scripts/rag_answer_eval.py` 即可拿到数字（小样本首跑：事实命中 3/3、忠实度 4.33/5、数字越界 0）。

### P3-19 文档与现实不一致

- `CLAUDE.md` 的结构图仍写着 `content_gen/`、`customer_service/` 是"空壳"（实际已是完整 Agent），且未体现新增的 `document` 链路与 `tools/document_tools`。
- `docs/` 实际只有 00、01、10、12、13，但 `00-项目概述.md` 的章节导航指向 02~09 六篇**不存在的文件**。
- **怎么修**：要么补齐章节，要么把导航改成"已发布章节列表"；`CLAUDE.md` 的结构图更新为当前实际。
- **状态**：✅ 已修（2026-09-13），选了"改导航"这条路（不硬凑不存在的章节）。
  - `docs/00` 的第六部分重写为**已发布章节**，只列真实存在的文件，所有站内链接逐个验证可解析。
  - `CLAUDE.md` 结构图按实际目录更新：`agents/`、`tools/document_tools`、`routes/documents|memory`、`db/models`、`infrastructure/doc_output.py`、`VizBlock.tsx`、`scripts/reconcile_memory|verify_*|rag_quality_eval`、`docs/`；"当前完成状态"推进到 09-13，补 `/api/memory`、`/api/documents`，并新增「增补（09-13）」一节记这一轮改了什么。
  - 顺手改掉两处**事实错误**：工具名应为 `query_sales` / `check_stock`（原写成 `sales_query` / `stock_alert`）、测试数 104 项（原写"100 项"，属估数）。`backend/services`、`db/migrations`、`infrastructure/cache|oss` 确认确实只有 `__init__.py`，"空壳"的描述是准确的，保留。

---

## 六、值得肯定的部分（本次评审确认）

1. **上轮提出的洞已闭环**：`registry` 现在统一做工具名 + 必填参数校验，`simple_agent._result_text` 会把 `error` 回传给 LLM —— "LLM 自我纠正"的回路真的通了（探针实测前两项通过）。
2. **角色拆分延续得很干净**：content / service / document 三个角色文件结构完全对称，supervisor 只做组装；从 3 条链路扩到 4 条只加了"一个文件 + 一段路由"。
3. **文档落盘的路径安全很扎实**：只取末段文件名、剥盘符、清 Windows 非法字符、`is_relative_to` 双保险 —— 手工验证穿越/清洗/白名单/隔离 **9/9 通过**。
4. **记忆系统方向正确**：从"删光重建"改成"SQLite 事实源 + 增量合并 + 双桶（semantic/episodic）+ 管理接口"，P1-4/P1-5 是这条路上的收尾活而非方向错误。
5. **`rag_eval.py` 就是"尺子"该有的样子**：golden set + 真值完整性自检 + 多策略 A/B，改检索前后可直接对比数字。
6. **测试基建建立**：离线 FakeLLM、不联网、不依赖 `.env` —— 这正是 `BaseLLM` 抽象的设计红利被兑现。

---

## 七、优化建议（按优先级的三条路线）

### 路线 A｜止血包（约半天，收益最高）　✅ 已完成

1. 删掉 `qwen.py` 的全局 `verify=False`（P0-1）；
2. `registry.execute` 包 try/except，把工具异常变成可读的 `ToolResult(success=False)`（P0-2）；
3. `echo=settings.debug` + 让 `database_url` / `jwt_expire_minutes` 真正生效（P1-7）。

> 收益：把"LLM 传错参数就整条崩"变成"LLM 自己改对重试"，是稳定性上最大的一次跃迁。

### 路线 B｜记忆收尾（约一天）　✅ 已完成

1. 合并加"作废比例"护栏 + 升级 0.45~0.9 档的判据（P1-4）；
2. 向量/DB 顺序对调或加 `reconcile_memory.py`（P1-5）；
3. importance 加衰减、episodic 加 TTL、兜底补数不参与热度（P2-8/9/10）；
4. 给合并逻辑补 DB 级测试（P3-17）。

### 路线 C｜文档闭环（约半天到一天）　✅ 已完成（下载接口 + 前端入口 + `.gitignore` 都已落地）

1. 下载接口（带归属校验）+ 前端下载入口 + `.gitignore` 加 `outputs/`（P0-3）；
2. 长期把本地落盘换成对象存储。

### 长期项（不紧急但值得排）

- ✅ Executor 工具白名单 + 打通 `uploaded_data`（P1-6）；
- ✅ 全局 token/时间预算与用量展示（P2-11）—— 收在 LLM 层（`BudgetedLLM` 装饰器），因为在 Graph 层看不到 ReAct 循环内部的调用量；
- ✅ 回答层评测 + 扰动测试（P3-18）—— 脚本与判据已就位，数值待账户恢复后重跑；
- ⬜ 部署：Docker Compose + 线上 demo（**仍未完成**：`docker/` 目录目前是空的）；
- 扩展方向：MCP server 包装 ToolRegistry、把角色 prompt 升级为可插拔 skills。

---

## 八、这份清单的"面试用法"

修复时的三条最有讲头的故事（都是**自己发现 → 定位 → 有数字/复现 → 修复**的完整闭环）：

1. **P0-2 工具异常穿透**：可以讲"我用探针脚本复现了 LLM 传中文相对时间导致整条链路崩，定位到 registry 没兜工具本体异常，改成返回结构化错误让 LLM 自我纠正"——这是对 Agent"韧性"的正面回答。
2. **P1-4 记忆合并的阈值实证**：可以讲"我用 difflib 做了相似度分档，然后用六组真实短句测出它分不出'关注退货率 vs 关注退款率'这类平行偏好，所以把判据升级/加护栏"——展示了"用数据验证设计假设"的习惯。
3. **P3-18 RAG 评测**：51 条 golden set + 四策略 + Recall@5/Hit@1/MRR，能直接回答"你怎么知道检索改好了"。

**不要回避第 P0-1 条**：如果面试官看到代码里的 `verify=False`，最好的回答是"我早期为了绕开 Windows 证书问题全局关过校验，后来意识到这是全进程的安全债，改成按请求传 CA 并移除了 monkey-patch"——把它讲成一次安全意识成长，而不是装没发生。

---

## 九、修复轮复盘（2026-09-13）

19 条全部改完之后回头看，真正值得记的不是"改了什么"，而是**这份清单自己也有三处不准**，都是动手时才发现：

1. **清单会过时**。P3-18 写"回答层评测仍为零"，但 `rag_quality_eval.py` 早就是回答层评测（扰动 + judge，docs/12 有 6/6 记录）；P3-19 里工具名写成 `sales_query`/`stock_alert`（实际 `query_sales`/`check_stock`）、golden 写成 55 条（实际 51）、测试数写"100 项"（实际 104）。
   教训：**动手前先验证"缺陷是否还存在"**，否则会花时间重造一个已经有了的东西。
2. **清单给的是方向，不是落点**。P2-11 建议"Graph 加全局 token/时间预算"，但调用量在 ReAct 循环内部累积、对图节点不可见 —— 收在 **LLM 层**做成 `BudgetedLLM` 装饰器才拦得住。照抄建议的位置会写出一个永远不触发的预算。
3. **尺子自己先错了**。P3-18 扰动测试第一版用字面短语 `"满 199 元包邮" in answer` 判"有没有走检索"，结果答案写成"满 **199 元** 即可享受包邮"，差点得出"系统在背预训练常识"的**错误结论** —— 实际是断言不准。改成抽语义点（正则取门槛数字）再与基线比对才对。
   教训：**修评测脚本时，先证尺子再测系统**；尺子错了，越"自动"越危险。

4. **修 bug 的代码自己也会带新 bug，而且测试可能刚好绕过它（09-14 补记）**。P2-13 那条"id 去重重编号"当时写成了
   `while tid in seen_ids: tid = f"t{len(seen_ids) + 1}"` —— `seen_ids` 只在**循环外**才 `.add()`，
   `len()` 在循环体内是常量 → 候选名固定不变 → 一旦那个名字已被占用就**同步 CPU 自旋**——
   堵的不是一个请求，而是整个事件循环（faulthandler 实测停在 `planner.py:80`）。
   而当时补的用例用的是 `[t1, t1]`，算出来的候选 `t2` 恰好空着，一次就过：
   **测试比触发条件宽松，等于没测**。触发形状是"重复的那条 id，它算出来的替换候选也已被占"，如 `[t1, t3, t3]`（LLM 完全可能这么给）。
   教训两条：① 给"防重复"类循环写用例，要构造**替换候选也被占**的形状，别用最顺手的 `[t1, t1]`；
   ② 任何循环都该有个"必然终止"的论证（修完这里的论证就是 `n` 严格递增，而 `seen_ids` 有限）。

遗留两项（都不在清单里，如实记着）：**部署**（`docker/` 仍为空）与 **P3-18 的完整数字**（DashScope 账户欠费，恢复后重跑）。

---

## 十、修复轮之后新发现的两条（2026-09-14）

这两条都**不在原 19 条里** —— 是修复轮收尾之后才自查出来的，两条都动了生产代码、都补了回归用例。
**为什么单开一节而不并进上面的编号**：原 19 条是那轮评审的坐标系，混进自己后发现的条目，"哪条是评审提的、哪条是后来发现的"就说不清了；反过来，如果只字不记，这份清单读起来会比实际完整。分开记，两边都保住。

共同点值得先说：**两条都出在"修复轮自己写的代码"上**，而且都是"异常/循环的边界"这一类 —— 不是功能没做对，是**出错时**和**极端输入**时的行为没想清楚。

### 新-1 · planner 的 id 重编号会死循环（P2-13 的修复本身带的洞）

- **现象**：喂 `[{"id":"t1"},{"id":"t3"},{"id":"t3"}]`（LLM 完全可以这么给），进程 **3 秒不返回**，faulthandler 栈停在 `planner.py:80` 的 `while` 那行。
- **要害**：这不是"某个请求变慢"，是**同步 CPU 自旋跑在请求路径上** —— 它占着事件循环，卡的不是一个请求，是**整台进程的所有并发请求**。异步服务里"循环体内有没有 `await`"直接决定故障半径。
- **原因**：`while tid in seen_ids: tid = f"t{len(seen_ids) + 1}"` —— `seen_ids` 只在循环**外**才 `.add()`，所以 `len()` 在循环体内是常量，候选名永远算同一个；一旦那个名字已被占用，条件恒为真 → 原地自旋。
  抽象成一句可迁移的判据：**循环里"推进的变量"和"判断的变量"不是同一个**（看着在循环体里改了状态，改的却是不参与条件的东西）。
- **为什么当时的用例没抓到**：补的用例是 `[t1, t1]`，算出的候选 `t2` 恰好空着，一轮就过 —— 它验证的是"重编号发生了"，**不是"重编号会终止"**。触发形状是"重复的那条 id，它算出来的替换候选也已被占"。
- **怎么修**：让候选名真的滚动 —— `n = len(seen_ids) + 1` / `while f"t{n}" in seen_ids: n += 1` / `tid = f"t{n}"`。终止性可证：`n` 每轮严格递增，`seen_ids` 有限。
- **验证**：修前 faulthandler 3 秒超时、栈精确落在 `planner.py:80`；修后同输入返回 `t1/t3/t4` 三条唯一 id，耗时 **0.0000s**。补回归用例 `test_duplicate_id_whose_replacement_is_taken_terminates`（就用 `[t1, t3, t3]` 这个形状）。另把 `backend/` 全部 `while` 扫了一遍（哨兵退出的、每轮重赋 offset 的、每轮 pop 收缩的），同类问题只此一处。
- **状态**：✅ 已修（commit `4ca611a`），坑已写进《10 · 踩坑总结》坑 23。

### 新-2 · 子任务异常没有隔离，一格炸掉整轮

- **现象**：`executor_node` 里是裸的 `asyncio.gather`（[supervisor.py:96](../backend/agents/supervisor.py#L96)）。原来只把 `BudgetExceeded` 接住做了降级，**其他异常一概没接**：LLM 500 / 超时（`qwen.py` 非 200 直接抛 `RuntimeError`）、工具内部报错、网络错误，任何一个子任务抛出来，都会穿过 `executor_node` → `AgentGraph.invoke`（`base.py:31` 没有 try）**直接中断** → `synthesize` 永远不执行。
- **影响**：用户看到的是"分析失败"，而**另外几个子任务其实已经查好了**，数据全被丢掉。这是多 Agent 最怕的失败模式 —— **并行度把可靠性拉低**：4 个并行子任务任一失败 = 整轮失败，可用性反而比串行更差。
- **实测到的机制**（和"异常会取消其余分支"的直觉不一样，值得记一笔）：`gather` **不取消**兄弟协程 —— 实测兄弟三个照样跑完（0.5s 后全部打印完成），只是**返回值被丢弃、token 白烧**。所以是"跑完了但白跑"，比取消更亏。
- **怎么修**：两层。
  1. `run_one` 里补 `except Exception` → 这一格就地降级成 `（该子任务失败：<异常类型>: <原因>）`，报告结构保持完整，synthesizer 还能把跑完的部分综合出来；
  2. `gather(..., return_exceptions=True)` 做第二道保险，**绝不让异常对象流进 `state["results"]`**，也绝不让它冒到图外面 —— 否则第 1 层就白做了。
- **顺带补的一个洞**：降级必须配日志。异常不再冒到 route 层，`chat.py` 那句 `logger.exception("Agent 链路异常")` 就兜不到了，于是给 supervisor 也接了同一个 logger 并 `logger.exception` —— 否则"偶发 500"就只剩报告里一句软话，没人在日志里查得到。**降级不等于免责**。
- **验证**：`test_non_budget_exception_is_isolated_to_its_subtask` —— 按 prompt 内容（而不是调用序号，子任务是并发的，序号归因会漂）向 t2 注入 `RuntimeError`，断言四件事：① 整轮不崩；② 两格都在（结构完整、没丢项）；③ **跑成功那格的结果保住**；④ 它真的进了综合报告的 prompt（不是只躺在 state 里）。
  **先证用例会失败**：把 `supervisor.py` stash 回改动前跑一遍，用例精确失败在 `supervisor.py:96 → invoke`，异常穿透路径和上面写的一模一样 —— 这样才算"复现测试"，而不是一条跟着代码一起变绿的摆设。
- **状态**：✅ 已修（commit `2e83a30`）。

