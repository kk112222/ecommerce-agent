# 00 · 项目概述

> 本系列笔记记录我从零到一做一个「电商运营 AI Agent」全栈项目的全过程。
> 目标是教别人也能一步步做出来，同时记录我踩过的每一个坑。

---

## 本章目标

读完这一章，你会明白：

1. 这个项目到底**做什么**
2. 它**给谁用**、解决什么问题
3. 为什么我用这些**技术栈**
4. 整体**架构**长什么样

---

## 一、这个项目是做什么的？

**一句话**：一个电商运营人员的 AI 助手，运营人员用自然语言提问，Agent 自动调用工具查数据、生成文案、查知识库。

举个例子，运营人员可以直接在聊天框里说：

```
"7月15号到今天，充电宝卖了多少？"
"帮我生成苹果15的三条淘宝标题"
"退货要多久，运费谁出？"
```

Agent 会自动：

1. 理解意图 → 决定调哪个工具
2. 调用工具查数据 / 生成内容 / 检索知识库
3. 综合分析 → 给出完整回答

> 重点：**这不是一个一问一答的聊天机器人**，而是一个能**自主规划、多步执行**的 Agent。

---

## 二、给谁用？解决什么问题？

**给谁用**：电商运营人员（内部使用，不是给顾客的）

**解决什么问题**：

- 运营查销售数据，不用再翻后台、写 SQL
- 运营写文案，不用从零憋字
- 运营答售后问题，不用翻文件夹找政策文档

---

## 三、为什么做这个项目？

<!-- TODO: 填上你真实的动机，这是面试官最想听的 -->

我的动机：

- 为了找 AI Agent 相关实习，需要一个能讲深讲透的完整项目
- 在了解和使用过langchain和langgraph，做了一个agent的demo之后，想要了解真实开发的要求和需求
- 想把学过的 Python + 数据库 + 前后端串成一个真正能跑的东西
- 想亲手实现一个"会干活"的 Agent，自己写的框架，而不只是调 API，调包

---

## 四、技术栈与为什么选它们

| 层级 | 技术 | 为什么选 |
|---|---|---|
| 后端框架 | FastAPI + Uvicorn | 异步性能好，自带 Swagger 文档 |
| LLM | 通义千问 DashScope | 国内可用，接口可切换 |
| 向量库 | Qdrant | 本地模式免部署，开发方便 |
| 数据库 | SQLite（开发）→ PostgreSQL（上线） | 开发简单，零配置 |
| ORM | SQLAlchemy 2.0 异步 | 原生支持 async |
| 前端 | React + TypeScript + Ant Design | 生态成熟，组件齐全 |

---

## 五、整体架构

```
用户（运营人员）
    │  自然语言提问
    ▼
[前端] React 聊天界面
    │  HTTP / SSE
    ▼
[API层] FastAPI
    │
    ▼
[Agent层] ReActAgent（while 循环，自主决定调工具）
    │  │
    │  ├─→ [工具1] 销售查询 ──→ SQLite
    │  ├─→ [工具2] 文案生成 ──→ LLM
    │  ├─→ [工具3] 知识库检索 ──→ Qdrant
    │  └─→ [工具4] 库存预警 ──→ SQLite
    │
    ▼
[LLM层] 通义千问（抽象接口，可切换）
```

**三层依赖关系**（这是整个项目的灵魂）：

```
Tool 层   ← Function Calling 的"手" —— 执行具体操作
Agent 层  ← 编排 + 决策的"大脑" —— 决定调什么、调几次
LLM 层    ← 思考的"发动机" —— 提供智能
```

---

## 六、章节导航

| 章节 | 内容 |
|---|---|
| [00 项目概述](00-项目概述.md) | 本项目（就是这章） |
| [01 LLM 抽象层](01-LLM抽象层.md) | 依赖倒置 + 工厂模式 |
| [02 工具系统](02-工具系统.md) | ToolSpec / BaseTool / Registry |
| [03 数据库设计](03-数据库设计.md) | SQLite + async ORM + 金额存分 |
| [04 ReAct Agent](04-ReAct-Agent.md) | 从一问一答到自主循环 |
| [05 混合检索 RAG](05-混合检索RAG.md) | BM25 + 向量 + RRF + 精排 |
| [06 API 与流式推送](06-API与流式推送.md) | FastAPI + SSE |
| [07 JWT 登录认证](07-JWT登录认证.md) | bcrypt + token + 门禁 |
| [08 前端](08-前端.md) | React + Ant Design |
| [09 总结与展望](09-总结与展望.md) | 收获、踩坑、下一步 |
| [10 踩坑总结](10-踩坑总结.md) | 全项目 17 个坑 + 面试速记 |

---

## 七、怎么运行这个项目

```bash
# 1. 配置环境变量（.env）
DASHSCOPE_API_KEY=你的key
JWT_SECRET_KEY=一串随机字符串

# 2. 初始化数据库 + 模拟数据
python scripts/init_db.py
python scripts/seed_data.py

# 3. 构建知识库（RAG）
python scripts/build_kb.py

# 4. 启动后端
uvicorn backend.api.app:app --host 0.0.0.0 --port 8000

# 5. 启动前端
cd frontend
npm install
npm run dev
```

---

## 本章小结

- 这是一个**给运营人员**用的 AI Agent，不是聊天机器人
- 核心是 **Agent 自主调用工具**，三层架构：Tool / Agent / LLM
- 技术栈选型都基于"**开发方便 + 能讲深**"

---

## 下一步

你已经知道项目是什么了。接下来读 [01 LLM 抽象层](01-LLM抽象层.md)，看看怎么把"调用哪个大模型"这件事设计得可切换。
